# NANNAI — template de variáveis de ambiente
# Copie para .env.development / .env.production conforme o modo.
# Apenas variáveis com prefixo VITE_ são expostas ao client.

VITE_API_BASE_URL=http://localhost:3333/api
VITE_APP_NAME=NANNAI Confeitaria e Padaria
VITE_USE_MOCK=false
