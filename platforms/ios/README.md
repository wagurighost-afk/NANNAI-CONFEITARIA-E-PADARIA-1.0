# iOS (Capacitor)

O target iOS é gerado no macOS com Xcode.

```bash
# No Mac, após npm install:
npx cap add ios
npm run cap:ios
```

Use as mesmas variáveis de `.env.capacitor.example` (`VITE_CLOUD_API_URL` apontando para a API Render).
