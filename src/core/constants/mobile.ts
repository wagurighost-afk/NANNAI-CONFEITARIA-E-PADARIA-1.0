/** Altura da barra inferior mobile (sem safe-area). */
export const MOBILE_BOTTOM_NAV_HEIGHT_PX = 64

/** IDs dos itens exibidos na barra inferior (mobile). */
export const MOBILE_BOTTOM_NAV_IDS = [
  'dashboard',
  'production',
  'recipes',
  'comments',
] as const
